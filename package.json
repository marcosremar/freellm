{
  "name": "freellm",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "vite@<6.4.2": "^6.4.2",
      "yaml@<2.8.3": "^2.8.3"
    }
  }
}
